// jest-dom adds the DOM matchers (toBeInTheDocument, toHaveTextContent, …) that the
// component assertions read naturally with.
import "@testing-library/jest-dom/vitest";

process.env.NODE_ENV = "test";
